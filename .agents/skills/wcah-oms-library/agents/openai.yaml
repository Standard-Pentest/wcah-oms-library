interface:
  display_name: "Wcah Oms Library"
  short_description: "Repo-specific patterns and workflows for wcah-oms-library"
  default_prompt: "Use the wcah-oms-library repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true